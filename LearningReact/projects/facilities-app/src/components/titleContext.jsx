import { createContext } from 'react';

export const TitleContext = createContext(null);

// 1. create a separate file with createContext and a new context
// 2. wrap (usually inside App.js) the istances
// 3. 
