import React from 'react';
import { Button } from '@mui/material';

function Navbar() {
  return (
    <Button variant="contained" size="big">
      <a href="facilitiesHome">Facility Home</a>
    </Button>
  );
}

export default Navbar;
