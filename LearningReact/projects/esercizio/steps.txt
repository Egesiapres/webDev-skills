1. npx create-react-app esercizio (cd projects)
  1.1 aggiungo gli import
  1.2 creo un functional o un class component (ormai i default sono i functional)
2. npm install axios (cd esercizio)
  2.1 in package.json, ora è stata aggiunta la voce axios
  2.2 aggiungo l'import di axios nel componente che serve a me
  2.3 inizializzo axios creando una variabile (api), 
    2.3.1 .create il quale al suo interno ha un oggetto (una delle sue proprietà è la base url)
    2.3.2
  2.4

3. Gestisco state e props a seconda di ciò che devo creare (barra di ricerca che restituisce l'istituto   medico desiderato)

4. MUI (pacchetto grafico di Google)  





  Installare mui material per grafica
  aggiungere un filtro per filtrare le facility per nome
  input in cui scrivo e mostro le facility in cui ho scritto

  filtro e UI
  axios async await sostituiscono il then e il catch
