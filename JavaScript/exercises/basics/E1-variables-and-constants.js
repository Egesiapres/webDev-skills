console.log('%cBASICS', 'font-weight: bold; color: aquamarine')
console.log('------ %c1: Variables & constants ❎', 'font-weight: bold;');

// E1
console.log(
  'E1: Declare a variable called "number" and assign it the value 5. Print the value of the variable on the console'
);

const number = 5;
console.log(number);

// E2
console.log(
  'E2: Declare a constant called "PI" and assign it the value of 3.14. Print the value of the constant on the console'
);

const PI = 3.14;
console.log(PI);

// E3
console.log(
  'E3: Declare a variable called "name" and assign it the value "Mark". Next, change the value of the variable to "Luke". Print the new value on the console'
);

let name = 'Mark';
name = 'Luke';

console.log(name);

// E4
console.log(
  'E4: Declare a variable called "greeting" and assign the value "Hello, how are you?" to it. Print the value of the variable on the console'
);

const greeting = 'Hello, how are you?';
console.log(greeting);

// E5
console.log(
  'E5: Declare a variable called "true" and assign it the value true. Print the value of the variable on the console'
);

const vero = true;
console.log(vero);

// E6 (optional)
console.log(
  'E6 (optional): Declare a variable called "numbers" and assign it an array containing the numbers 1 to 5. Print the array on the console'
);

const numbers = [1, 2, 3, 4, 5];
console.log(numbers);

// E7 (optional)
console.log(
  'E7 (optional): Declare a variable called "person" and assign it an object with the following properties: "name" (with value "Mario") and "age" (with value 30). Print the object on the console'
);

const person = {
  name: 'Mario',
  eta: 30,
};

console.log(person);
